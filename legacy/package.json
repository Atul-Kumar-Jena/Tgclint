{
  "name": "fluid-glass",
  "version": "1.0.0",
  "description": "Fluid Glass — editorial-luxury marketing site for architectural glazing. Self-contained static build (custom smooth-scroll + motion engine, canvas fluid-glass hero, SVG art direction).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs && python3 -m http.server 8080 --directory dist",
    "check": "node build.mjs && node tools/smoke.mjs"
  },
  "license": "MIT",
  "private": true
}
